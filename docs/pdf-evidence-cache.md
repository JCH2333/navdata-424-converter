# PDF 证据缓存

全量 2608 图表提取耗时较长。`convert --pdf-cache <目录>` 可将已解析的 PDF 证据保存在候选输出旁的本地目录；缓存绝不写入 NAIP 原始目录，也已被 Git 忽略。

缓存键包含 PDF SHA-256、机场、图表分类、索引标题、解析器名称和缓存格式版本。命中后恢复的对象与首次解析时的 `ProcedureChart`、来源页码/PDF 哈希、航段属性和坐标逐字段一致。PDF 内容或解析上下文改变时会自动重新解析。

缓存是性能优化，不是数据来源：删除后仅会导致重新提取，不能使任何尚未可靠识别的程序通过转换验证。
